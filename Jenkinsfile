pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // No other actions, just echo
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // No other actions, just echo
            }
        }
    }
}
