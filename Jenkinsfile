pipeline {
    // agent any
    
    // tools {
    //     nodejs 'NodeJS' // Name as configured in Global Tool Configuration
    // }
    
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // bat 'npm install'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // bat 'npm test'
            }
        }
    }
}
