pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // No need for any shell commands since you just want to echo
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // No need for any shell commands since you just want to echo
            }
        }
    }
}
